<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 通知权限 (Android 13+ 运行时) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ★ ColorOS 关键: 提升通知权限 (允许通知进入流体云) -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />

    <!-- AlarmManager 精确闹钟 (用于服务保活 tick) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 开机自启 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 忽略电池优化 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".FluidCloudApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColorOSFluidCloud"
        android:usesCleartextTraffic="true">

        <!-- 主界面 Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ColorOSFluidCloud">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 关于页面 -->
        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.ColorOSFluidCloud" />

        <!-- ColorOS 16 流体云前台服务
             使用 specialUse 类型;
             通过 AlarmManager tick + NotificationManager.notify() 实现常驻
             (而非直接 startForeground, 避免白圈问题) -->
        <service
            android:name=".service.FluidCloudService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- 开机自启广播接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
